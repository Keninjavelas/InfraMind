apiVersion: apps/v1
kind: Deployment
metadata:
  name: privileged-deployment
spec:
  template:
    spec:
      containers:
      - name: root-container
        image: nginx:1.14.2
        securityContext:
          privileged: true
